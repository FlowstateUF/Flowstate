# Flowstate

## Guide
- make sure all secrets exist in backend/.env
- install docker desktop and open
- run `docker compose up -d` for containers to run in background (detached, no logs)
- run `docker compose up` for containser to run in foreground (logs)

## Redis
- you can go to localhost:8081 to see redis jobs